flowchart TB
    subgraph Client[Client]
      FE[Frontend / Vite / React]
    end

    subgraph Services[Runtime Services]
      BE[Backend / Express]
      RT[Realtime / ws + Yjs relay]
      AIS[ai-service provider adapter]
    end

    subgraph Data[Persistence]
      DB[(SQLite)]
    end

    FE -->|REST| BE
    FE -->|WebSocket| RT
    BE -->|reads/writes| DB
    RT -->|permission + revert polling| DB
    BE -->|imports provider logic| AIS
